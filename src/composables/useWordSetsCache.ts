import type { Res } from "@types";
import type { Ref } from "vue";
import { ref } from "vue";
import { FetchStatuses } from "/src/FetchStatuses";
// type?: KeyOfType<Response, Function>;

export default function useWordSetsCache<T>({}: {}) {
    return {};
}
